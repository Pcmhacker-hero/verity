/** Tier 2: Semantic checker — Doc 11 §6 step 4. LLM-based semantic verification. */
export class SemanticChecker {
  async check(_batch: unknown) {
    // TODO: Build prompt from batch, call LLM, parse findings
    return [];
  }
}
