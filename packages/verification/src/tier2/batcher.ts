/** Tier 2: File batcher — Doc 18 §8.2. Groups files by spec area for LLM batching. */
export function batchFilesBySpecArea(_files: Map<string, string>) {
  // TODO: Group files by heuristic (auth/, models/, routes/, other)
  return [];
}
